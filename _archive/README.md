# _archive — What Was Archived and Why

Nothing here was deleted. Everything is untouched, moved as-is from its original location, so the reconciliation is reversible if a judgment call turns out wrong.

| Item | Why archived |
|------|-------------|
| `Starter project pack (source, absorbed into 01-framework, 02-cheatsheet, 03-setup, 04-worked-example)/` | The winning, most-complete source (dated 2026-06-08). Its content was reorganized and lightly edited (renamed files, fixed cross-references, merged the duplicate session-bootstrap section) into `../01-framework/`, `../02-cheatsheet/`, `../03-setup/`, and `../04-worked-example/`. Kept whole for provenance/diffing. |
| `Agentic WorkFlow (superseded snapshot, 2026-05-15)/` | An older, sparser duplicate of `Starter project pack/` — verified by diff to be a strict subset of the winning copy (missing the QA workflow integration, session-start protocol, and industry-standards sections). One piece of unique value was salvaged before archiving: its `Starter project pack/Start.txt` (a complete, generic master prompt) became the basis for `../01-framework/master-prompt.md`. |
| `claude-code-cheatsheet.md (source, copied unchanged into 02-cheatsheet)` | Byte-identical to `../02-cheatsheet/claude-code-cheatsheet.md` — kept here only so the original root layout is fully accounted for. |
| `AgentE2EQAWorkflow-Playwright-main.zip` | Hash-verified (SHA-256) byte-identical to the already-extracted, actively-developed folder now living at `../04-worked-example/AgentE2EQAWorkflow-Playwright-main/`. A second identical copy and a third zip (`End to End Agentic Qa AI workflow.zip`, itself containing a hash-identical copy of this same zip) were removed along with the whole `Agentic WorkFlow/` folder above — not duplicated here a third time. |
| `Master Prompt.txt (project-specific lpr-client-portal example)` | Despite its name, this was a one-off kickoff message hardcoded to an unrelated project path (`lpr-client-portal`), not a reusable template. The genuinely generic master prompt (`Start.txt`, from the `Agentic WorkFlow/` copy) was promoted to `../01-framework/master-prompt.md` instead. Kept here as an example of a filled-in kickoff message. |
| `EXECUTIVE_SUMMARY OF WHAT WAS BUILT.md` | A point-in-time build changelog (dated 2026-06-08) documenting the session that added `/init` — historical/provenance record, not instructional content end users act on. |
| `ai_claude_code_consolidated_notes.md` | Mixed content transcribed from screenshots. Its "Claude Code: 12 Features" section was redundant with, and less detailed than, `claude-code-cheatsheet.md`. Its distinct AI-terms glossary and agentic-systems-layers diagram were folded into `../02-cheatsheet/ai-glossary.md`, clearly labeled as unsourced background reference. Full original kept here. |
| `promptScribble..txt` | Personal scratch notes. One fragment — a "verify via real UI/DOM interaction, not simulated success" principle — was extracted into `../01-framework/global-rules.md`. The rest (unrelated Google Auth / LemonSqueezy / Cloudflare deployment notes for a different product) has no place in this framework. |

An empty `Prompts/` folder (no files) and the folder-structure difference from the original task brief (no screenshot files were ever found in this tree) are noted for the record but produced no archivable content.
